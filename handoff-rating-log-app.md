# 引き継ぎ資料:汎用評価記録アプリ

Claude(チャット)でのプロトタイプ検証を経て、Claude Codeでの本実装に引き継ぐための資料。

## 背景・経緯
- もともとは蕎麦・ビール・日本酒の味の記録アプリとして着想
- チャット内でHTMLプロトタイプ(`tasting-log.html`)を作成し、ユーザーが実際に触って要件を確認済み
- プロトタイプを使ってみた結果、「食べ物専用」ではなく、本・旅行先・釣り場・駅の住みやすさなど、**任意のジャンルに評価軸を設定して記録する汎用サービス**にしたいという方向に発展
- 添付の `tasting-log-spec.md`(初期版仕様)と `tasting-log.html`(プロトタイプ本体)も参照のこと。本ドキュメントはその更新版・統合版

## コンセプト
「ジャンルを自分で定義し、評価軸(n個)を自分で設定して、対象(店・本・場所など)を評価・記録していく」ための汎用ツール。
例:
- 蕎麦:コシ、出汁、蕎麦感、天ぷら、蕎麦湯
- ビール:苦味、香り、キレ、コク、喉ごし
- 本:文章力、構成、読後感、独自性、再読性
- 釣り場:釣果、アクセス、混雑、景観、設備

## 機能要件

### ジャンル管理
- ユーザーが自由にジャンルを作成できる(名称は自由入力、食べ物に限定しない)
- ジャンルごとに評価軸をn個自由に定義できる
- 評価軸のスケールは現状5段階固定(プロトタイプ)。将来的に段階数を可変にするかは要検討

### 記録機能
1レコードが持つ項目:
- ジャンル
- 対象名(店名・書名・地名など、ジャンルにより意味が変わる自由記述)
- 日付
- 写真(1枚、プロトタイプではリサイズ+JPEG圧縮しBase64で保持)
- 評価軸ごとのスコア
- タグ(複数、自由入力)
- コメント(自由記述)
- 公開/非公開フラグ

### 検索・一覧
- 自分の記録をジャンル別フィルタ、キーワード検索(対象名・タグ・コメント)で絞り込み

### 公開範囲
- レコード単位で公開/非公開を切り替え
- 非公開:本人のみ閲覧可
- 公開:他ユーザーも閲覧できる一覧に表示
- ユーザーごとにデータが明確に分離されていること(他人の非公開データは絶対に見えない)

## プロトタイプでの実装知見(参考)
- Claudeのアーティファクト内蔵ストレージ(`window.storage`)を使用。personal/sharedの2種類のスコープがあり、公開レコードのみsharedにも複製する設計にした
- ジャンルは `genres` キー、自分の全記録は `entries` キーにJSON配列で保存。公開レコードは `public:{id}` というキーでshared保存
- デザイン:和紙(washi)背景、墨(sumi)テキスト、藍(ai)アクセント、印鑑(hanko)モチーフの丸いスタンプで記録数を表示、明朝体(Shippori Mincho)+ゴシック(Zen Kaku Gothic New)の組み合わせ
- このプロトタイプの制約:単一デバイス・単一セッション内でのみ有効。マルチデバイス同期やアカウントを跨いだ永続化はできない

## 本実装で検討・決定してほしい事項

### 認証・アカウント
- 自作の認証(パスワードの自前管理)は避け、Firebase Auth / Supabase Auth / Clerk などの既存認証サービスの利用を推奨(検討候補として提示。技術選定はお任せ)
- 通信はHTTPS前提

### データ設計
- ジャンル・評価軸・レコードのテーブル設計(RDB想定なら Genre / Axis / Entry / User / Tag などのテーブル分割)
- 写真はBase64埋め込みではなく、S3やSupabase Storageなど専用の画像ストレージに保存しURL参照する設計を推奨(プロトタイプはBase64だが本実装には非推奨)

### 公開範囲・プライバシー
- レコード単位の公開/非公開フラグをDBレベルでアクセス制御(RLS等)と連動させ、非公開データが他ユーザーのクエリに絶対に混入しない設計にすること

### スケール・収益化
- 現時点では優先度低。将来的に広告等でのマネタイズも検討し得るが、まずはプロダクトの使用感・継続率を優先する方針(ユーザーの意向)

## 未決事項(要ヒアリング・要決定)
- サービス名称(「味帖」から汎用名への変更含む)
- 評価軸のスケールを可変にするか
- 複数写真対応の要否
- 公開一覧の見せ方(フィード形式・ランキング形式など)
- 対応プラットフォーム(まずはWeb/PWAのみか、将来ネイティブアプリ化するか)
