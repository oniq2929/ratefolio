# テイスティング記録アプリ 仕様書

## 概要
蕎麦・ビール・日本酒など、食べたもの/飲んだものの味の記録が記憶に残らないという課題を解決するための個人用記録アプリ。
ジャンルごとに評価軸を自由に設定し、店名・日付・写真とともに自分なりの評価を記録する。

## 想定プラットフォーム
- iPhoneで使うWebアプリ(PWA想定、ホーム画面追加でアプリ的に利用)
- 将来的にネイティブアプリ化する可能性はあるが、まずはWebアプリで検証

## 主要機能

### 1. ジャンル管理
- ジャンルを自由に作成できる(例:蕎麦、ビール、日本酒)
- ジャンルごとに評価軸(n個)を自由に定義できる
  - 例(蕎麦): コシ、出汁、蕎麦感、天ぷら、蕎麦湯
  - 例(ビール): 苦味、香り、キレ、コク、喉ごし
  - 例(日本酒): 甘辛、香り、コク、キレ、後味
- 評価軸は数値評価(例: 5段階 or 10段階)を想定

### 2. 記録機能
各記録(投稿)に持たせる項目:
- ジャンル(どのジャンルに属するか)
- 店名/銘柄名などの自由記述
- 日付(記録日、デフォルトは当日)
- 写真(1枚以上)
- 評価軸ごとのスコア(そのジャンルに設定された評価軸すべてに対して入力)
- タグ(自由入力、複数付与可)
- コメント/メモ(自由記述、任意)
- 公開設定(公開 / 非公開)

### 3. 検索・一覧機能
- タグでの検索・絞り込み
- 店名での検索
- ジャンルでの絞り込み
- 日付での絞り込み・並び替え
- 自分の過去の記録を一覧で振り返れる

### 4. データの公開範囲
- 各記録ごとに「公開」「非公開」を選択できる
- 非公開:投稿した本人のみ閲覧可能
- 公開:他のユーザーも閲覧可能な一覧に表示される
- 個人のデータは他者のデータと混ざらないこと(ユーザーごとにデータが分離されていること)

## データ構造(イメージ)

```
Genre:
  - id
  - name (例: 蕎麦)
  - axes: [ { id, name (例: コシ), scale (例: 1-5) }, ... ]

Entry:
  - id
  - genreId
  - storeName
  - date
  - photos: [photoId, ...]
  - scores: { axisId: value, ... }
  - tags: [tag, ...]
  - comment (optional)
  - isPublic: true/false
  - ownerId (記録した本人の識別子)
```

## 非機能要件
- 個人データと共有(公開)データを明確に分離して扱う
- ユーザーは自分の非公開データを他者から見られない
- スマホでの片手操作を想定した入力UI(評価軸の入力はタップ操作で完結するのが望ましい)

## 未決事項(今後検討)
- 評価軸のスケール(5段階 / 10段階 / 自由入力など)を統一するか、ジャンルごとに変えられるようにするか
- ユーザー認証の方式(メール/Apple ID/匿名など)
- 公開データの閲覧UI(ランキングやフィード形式にするかどうか)
- 写真の保存方法・容量制限
