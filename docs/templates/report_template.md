<!--
====================================================================
報告書テンプレート仕様 v1.0
このファイルは「報告書(report)」を生成する際にClaude Codeが
参照するテンプレートです。以下の [ ] で囲まれた指示に従い、
実際のコードに即した内容で本文を埋めてください。

文体: である調（敬語不可）。社内技術報告書として簡潔・客観的に書く。
図表: Mermaid等の記法は使わず、figures/ 配下にPNG/SVGとして
      書き出し、![説明](figures/xxx.png) の形で参照する
      （Word変換時に確実に埋め込むため）。
出力先: reports/{作成コード名}_report.md

【YAMLフロントマター】
Obsidianでの整理・グラフ表示・Dataviewクエリのために、本文冒頭に
以下のYAMLフロントマターを必ず付与すること。
- type は report 固定
- project はツール名（C:\claude_dev\{ツール名}\ のツール名と一致させる）
- tags には "type/report" と "project/{ツール名}" を最低限含める
  （関連する技術要素があれば "tech/xxx" も追加してよい）
- related には対応する knowledge ノートへの [[wikilink]] を必ず1つ以上含める
  （knowledge側にも相互にリンクを張ること）
- version はYAML側にも記載し、本文の改訂履歴表と値を一致させること
- created は初版作成日から変更しない。updated は更新の都度上書きする
====================================================================
-->

---
title: "{作成コード名} 報告書"
type: report
project: "{ツール名}"
created: "{YYYY-MM-DD}"
updated: "{YYYY-MM-DD}"
version: "v1.0"
tags:
  - type/report
  - "project/{ツール名}"
related:
  - "[[{作成コード名}_knowledge]]"
aliases:
  - "{作成コード名}"
---

# {作成コード名} 報告書

| 項目 | 内容 |
|---|---|
| 作成日 | {YYYY-MM-DD} |
| 対象コード | `{ファイルパス}`（バージョン/コミット: {commit_hash}） |
| 作成者 | {氏名} |
| 版数 | v1.0 |

## 改訂履歴
| 版 | 日付 | 変更内容 |
|---|---|---|
| v1.0 | {YYYY-MM-DD} | 初版作成 |

---

## 1. 目的・対象読者

[ 本報告書の目的（何を承認/共有/引き継ぎするためのものか）と、
  想定読者（社内の同分野の技術者を想定、を基本とする）を記す。]

## 2. 背景

[ このコードを作成するに至った経緯、解決したい業務・研究上の課題を記す。
  である調で簡潔に。]

## 3. 数学的・物理的な前提知識

[ 本コードの理解に必要な理論的背景を、社内の技術者向けに簡潔にまとめる。
  説明書と異なり、教育的な丁寧さより要点の網羅性を優先する。
  数式はKaTeX形式（$...$ / $$...$$）で記述する。]

## 4. 仕様

### 4.1 入力仕様
[ 入力データの形式・型・想定される範囲・単位などを表形式で。]

| 項目名 | 型 | 説明 | 制約 |
|---|---|---|---|
| | | | |

### 4.2 出力仕様
[ 出力データの形式・型を表形式で。]

| 項目名 | 型 | 説明 |
|---|---|---|
| | | |

### 4.3 制約条件・前提条件
[ 動作を保証する条件（データ量、精度要件、想定する使用環境など）を記す。]

## 5. 使用ライブラリ・実行環境情報

| 項目 | 内容 |
|---|---|
| 言語/バージョン | |
| 主要ライブラリ | {ライブラリ名 : バージョン} |
| 実行環境 | {OS, ハードウェア等} |
| 実行方法 | {コマンド例} |

## 6. アルゴリズム

[ 処理手順を番号付きで簡潔に記述する。数式の導出過程ではなく、
  「何をどの順番で計算するか」という手続きを示す。]

1. {手順1}
2. {手順2}
3. {手順3}

## 7. フローチャート

[ 処理の流れを図示したものを figures/ 配下に用意し、以下のように挿入する。]

![処理フロー](figures/{作成コード名}_flowchart.png)

## 8. 実装

[ 実装の要点（構成、主要な関数・クラスの役割）を簡潔に説明する。
  コード全文はAppendixに回し、ここでは設計の要点のみを述べる。]

| 関数/クラス | 役割 |
|---|---|
| | |

## 9. 出力結果

[ 実際に実行して得られた結果を示す。数値表・グラフ画像など。]

![出力結果](figures/{作成コード名}_result.png)

## 10. 妥当性検証

[ 出力結果が正しいと判断できる根拠を示す。
  例: 既知の解析解・実験値との比較、単体テストの結果、境界値での挙動確認等。
  検証しなかった範囲があれば明記する。]

| 検証項目 | 方法 | 結果 | 判定 |
|---|---|---|---|
| | | | |

## 11. 既知の制限事項・今後の課題

[ 現状の実装が対応できていない範囲、精度上の限界、
  今後改善すべき点を列挙する。]

## 12. 総括

[ 本コードで達成したことを、である調で簡潔にまとめる。]

## 13. 参考文献

[ 参照した文献・資料をリスト化する。]

---

## Appendix. コード実例

```python
# コード全文または主要部分の抜粋
```
