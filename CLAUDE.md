# CLAUDE.md

## 1. プロジェクト概要

ジャンルを自分で自由に定義し、ジャンルごとに評価軸（コシ・出汁・蕎麦感…等）をn個自由に設定して、対象（店・本・旅行先・釣り場・駅など任意のもの）を評価・記録していく汎用サービス。食べ物専用ではなく、本・旅行先・釣り場・駅の住みやすさなど、あらゆるジャンルの評価記録に使えることを目指す。旧称「味帖」。詳細仕様は `handoff-rating-log-app.md`（引き継ぎ資料）を参照。

## 2. 技術スタック・実行環境

| 項目 | 内容 |
|---|---|
| 言語/バージョン | 未確定（着手前にユーザーと相談して決定すること） |
| 主要ライブラリ | 未確定 |
| 環境構築方法 | 未確定 |
| 実行方法 | 未確定 |

- 認証は自作せず、Firebase Auth / Supabase Auth / Clerk 等の既存認証サービスを利用する方針（技術選定自体は着手時に要相談）。
- 写真はBase64埋め込みではなく、S3やSupabase Storage等の専用画像ストレージにURL参照で保存する方針。
- 通信はHTTPS前提。

## 3. ディレクトリ構成

```
C:\claude_dev\rating-log-app\
├── src/                      # コード本体
├── docs/
│   └── templates/
│       ├── knowledge_template.md
│       └── report_template.md
├── knowledge/                # 説明書の出力先（学習用）
├── reports/                  # 報告書の出力先（社内共有用）
├── figures/                  # 報告書に埋め込む図（PNG/SVG）
├── tests/                    # テストコード（あれば）
└── CLAUDE.md
```

## 4. Git運用ルール（全ツール共通・固定）

- コードは必ずGitで管理すること。作業前にリポジトリが未初期化であれば
  `git init` から行い、`.gitignore` を用意すること
  （少なくとも `__pycache__/`, `.venv/`, 生成物の一時ファイル等を除外）。
- 意味のある単位（1機能・1修正ごと）でコミットすること。
  「まとめて最後に1コミット」は避ける。
- コミットメッセージは日本語で可。内容が分かる一文＋必要であれば箇条書きで詳細。
- mainへの直接コミットで可（個人開発のため）。ただし大きな実験的変更を
  試す場合は、featureブランチを切ってから作業してよい。
- 破壊的な操作（force push、履歴の書き換え等）は必ず事前に確認を取ること。

## 5. ドキュメント生成・更新ルール（全ツール共通・固定）

- コードを新規作成・修正した際は、`docs/templates/knowledge_template.md`
  および `docs/templates/report_template.md` の形式に**厳密に従い**、
  以下を生成・更新すること。
  - `knowledge/{コード名}_knowledge.md`（学習用の説明書）
  - `reports/{コード名}_report.md`（社内共有用の報告書）
- 報告書内で使う図は `figures/` 配下にPNG/SVGとして書き出し、
  Markdown内では画像参照（Mermaid等の記法は使わない）とすること。
- コード完成後にまとめて一度だけ作成するのではなく、機能実装や大きな修正の
  区切りごとに都度、説明書・報告書を更新すること。
- 既存のドキュメントを更新する場合は、報告書側は改訂履歴の表に
  変更内容を追記すること。
- 生成後、テンプレートの各章を埋め切れているか自己チェックしてから
  提示すること（空欄の `{ }` が残っていないか等）。

## 6. コーディング規約

- 関数・変数名は英語、コメントは日本語で可。
- ユーザーはアプリ開発初心者のため、実装方針や専門用語は都度わかりやすく説明しながら進めること。

## 7. テスト方針

- 現時点ではテスト未整備。データ分離（自分の非公開データが他ユーザーから見えないこと）と公開/非公開の切り替えは特に事故が起きやすい箇所のため、実装時に重点的に確認すること。

## 8. このツール固有の注意事項

- ジャンル・評価軸（各n個）・対象名・日付・写真・タグ・コメント・公開/非公開フラグを持つレコード構造とすること（詳細は引き継ぎ資料参照）。
- レコード単位の公開/非公開はDBレベルのアクセス制御（RLS等）と連動させ、非公開データが他ユーザーのクエリに絶対に混入しない設計にすること。
- 収益化（広告等）は現段階では検討不要。まずプロダクトとしての使用感・継続率を優先する。
- サービス名称（「味帖」から汎用名への変更含む）、評価軸のスケールを可変にするか、複数写真対応の要否、公開一覧の見せ方、対応プラットフォーム（Web/PWAのみか将来ネイティブ化するか）は未決事項。着手前にユーザーと相談すること。

## 9. 作業時の振る舞いに関する指示（全ツール共通・固定）

- 既存コードを大きく書き換える場合は、変更方針を先に説明してから着手すること。
- 外部ライブラリの新規追加は、追加理由を一言添えること。
- 不明点がある場合は、推測で進めず質問すること。
